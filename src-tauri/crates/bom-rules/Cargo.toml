[package]
name = "bom-rules"
version = "0.0.1"
edition = "2021"
description = "Deterministic BOM checks (the free tier) — emits findings.json v1.0"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
blake3 = "1"
