{
  "profile": "commercial",
  "comment": "R1 is duplicated, R? is unannotated, C1 has no footprint (one part of six, so per-item not systemic), C2 has quantity 0.",
  "expect_rules": [
    "bom.duplicate_refdes",
    "bom.unannotated_refdes",
    "bom.missing_required_field",
    "bom.invalid_quantity"
  ],
  "forbid_rules": ["bom.missing_sourcing_id", "bom.placeholder_part_number"]
}
