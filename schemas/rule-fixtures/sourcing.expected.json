{
  "profile": "default",
  "comment": "C1 has a placeholder MPN; C2 is sourced by LCSC only; U1 has a manufacturer but no MPN; C2's LCSC code breaks the C-digits pattern; R3's datasheet is a local Windows path.",
  "expect_rules": [
    "bom.placeholder_part_number",
    "bom.distributor_pn_only_no_mpn",
    "bom.manufacturer_mpn_unpaired",
    "bom.supplier_pn_format",
    "bom.invalid_datasheet"
  ],
  "forbid_rules": ["bom.missing_sourcing_id", "bom.duplicate_refdes"]
}
